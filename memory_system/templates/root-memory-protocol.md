# Workspace Memory Protocol

This workspace is `$root_identity`. Resolve the exact project from `_memory/Context/project-registry.md` before using memory.

Read that project's current authority files and state document before non-mechanical decisions. Then use the official claude-mem service in this order: project-scoped search → timeline → full observation retrieval; omit platform filters from the search.

Agents must not recall from sibling projects. Source precedence is current project authority, then current project state, then verified project-scoped claude-mem observations; higher-precedence sources win conflicts. If claude-mem is unavailable, pause consequential work.

The workspace state document is `$root_state`.
